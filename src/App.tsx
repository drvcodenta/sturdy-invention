import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="app-panel">
        <p className="app-label">Workspace Reset</p>
        <h1>Ready for fresh component specs</h1>
        <p className="app-subtitle">
          All previous demo code has been removed. Share the next requirements and we will
          build the interface step-by-step.
        </p>
      </section>
    </main>
  )
}

export default App

function App() {
  return (
    <div className="min-h-screen bg-[#f7f8fb] flex items-center justify-center px-6">
      <button
        type="button"
        className="rounded-full border border-blue-500/60 bg-blue-600 px-6 py-2 text-sm font-semibold text-white"
        aria-label="Open nested menu"
      >
        <span>Open Menu</span>
      </button>
    </div>
  )
}

export default App
